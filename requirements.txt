# Core scientific stack
numpy>=1.24,<2.0
pandas>=1.5
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0
tqdm>=4.65

# Feature extraction
pyradiomics @ git+https://github.com/AIM-Harvard/pyradiomics.git
SimpleITK>=2.2

# Plotting / evaluation
matplotlib>=3.7
seaborn>=0.12

# XGBoost 
xgboost>=2.0

# Deep tabular models
torch>=2.1
tabicl>=0.1.0
# TabFM (Google Research, v1.0.0 checkpoint) — install per upstream repo instructions;
# pinned here to the release used in this pipeline.
# tabfm @ git+https://github.com/google-research/tabfm.git@v1.0.0